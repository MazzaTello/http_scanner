# HTTP Request Tool
This is a simple Python project. It sends an HTTP request to a URL and shows basic results like status code, headers and content preview. You need Python to run it.

Install libraries:
pip install requests colorama

Important:
You must enter URL with http:// or https:// or the program will not work.

Run the program:
python main.py


This project is for learning and practice.